# NoBreakZone — 작업 규칙

Core Keeper 모드. 상세 문서는 `Editor/Docs/` 아래에 있다.
읽는 순서: 이 문서 → `Editor/Docs/status.md` → `Editor/Docs/design.md`.

---

## 0. 최우선 규칙 — 플랜 먼저

**사용자가 "바로 해", "그냥 해"처럼 명시적으로 지시하지 않는 한, 어떤 작업이든
플랜모드를 거친다.** 작업의 크기·난이도는 판단 근거가 아니다. 한 줄 수정도 예외 없다.

- 조사·읽기·grep·질문은 승인 없이 해도 된다. 오히려 권장한다.
- **파일을 쓰는 순간부터** 승인된 플랜이 있어야 한다.
- 결정할 항목이 남아 있으면 코드가 아니라 질문을 먼저 내놓는다.
- **사용자가 질문에 답한 것은 구현 시작 승인이 아니다.** 승인은 플랜 승인뿐이다.
- 승인된 플랜의 범위를 벗어나야 하면 멈추고 다시 묻는다.

플랜에는 반드시 들어간다 — 무엇을 바꾸는가(한 문장) · 건드릴 파일과 새로 만들 파일 ·
**사람이 게임에서 무엇을 확인해야 하는가** · 예상되는 위험이나 불확실한 부분 · 기획서의 어느
항목에 해당하는가. 기획서에 없는 작업이면 그 사실을 먼저 밝히고 승인을 받는다.

---

## 1. 이 저장소의 구조적 제약

저장소 루트가 곧 유니티의 `Assets/NoBreakZone/`, 즉 **모드 번들 경로(modPath)**다.
이게 파일을 어디에 둘지를 전부 규정한다.

1. **modPath 아래 거의 모든 자산이 번들에 실린다.** `ModBuilder`는 `.cs`·`.dll`·`Conf/`·
   `Localization/`·`Editor/`·`CodeGen/`을 **제외한 전부**를 AssetBundle로 쓸어담는다.
   → **배포 대상이 아닌 것은 `Editor/` 아래에 둔다.** 루트에는 파일을 늘리지 않는다.
   (근거: `Editor/Docs/research.md` 6장. 실제로 초안 PNG와 `sprites.py`가 번들에 실려 사고가 났다.)
2. **모든 자산에 `.meta`가 붙는다.** 이름을 바꾸거나 옮길 때는 `.meta`도 **반드시 같이**
   옮긴다. 안 그러면 유니티가 GUID를 새로 발급해 참조가 끊긴다. 새 폴더를 만들면 폴더
   `.meta`도 함께 만든다(`folderAsset: yes` + 신규 GUID).
3. **점(`.`)으로 시작하는 폴더는 유니티가 자산으로 읽지 않는다.** `.claude/`·`.github/`는
   modPath 안에 있어도 번들과 무관하고 `.meta`도 필요 없다.
   `~`로 끝나는 폴더(`Editor/LogicTests~`)도 같다.
3-1. **`Prefabs/`·`Textures/`·`Data/`는 손으로 고치지 않는다.** `Editor/genassets.py`가 소유하고 매번
   덮어쓴다. 스펙을 고치고 재실행한다. 그림 원본은 `Editor/Docs/art/`다. 예외는 폴더 `.meta` —
   한 번 만들고 다시 안 건드린다. guid가 바뀌면 그 아래 참조가 전부 끊긴다.
4. **파일·폴더명은 ASCII.** 맥/윈도우를 오가는 저장소라 한글 경로는 유니코드 정규화
   사고를 낸다. 파일 *내용*의 언어는 아래 언어 표를 따른다.

---

## 1-1. 언어 — 여기가 유일한 원본

**코드 · 이력 · 배포물은 영문. 작업 문서는 한글.**

기준은 하나다. **이 저장소를 한국어를 모르는 사람이 연다고 가정한다.** 그 사람이 먼저
부딪히는 것은 전부 영문이어야 하고, 나와 AI만 읽는 작업 문서는 한글이 빠르다.

| 대상 | 언어 |
| --- | --- |
| 커밋 subject·본문 | **영문** |
| PR 제목·본문 | **영문** |
| 릴리스 노트 | **영문** |
| `README.md` · `LICENSE.md` | **영문** |
| 코드 주석 | **영문** |
| `Editor/Docs/**` 설계 문서 | 한글 |
| `CLAUDE.md` · `.claude/skills/**` | 한글 |
| 파일·폴더명 | ASCII |

커밋 본문까지 영문인 이유는 `git log`·`git blame`·GitHub 커밋 뷰에 그대로 노출되고,
PR 본문이 커밋 본문의 요약이라 언어가 갈리면 매번 번역이 되기 때문이다.

**이미 한글로 쓰인 과거 커밋은 고치지 않는다.** 이력은 기록이다.

다른 문서에서 언어 규칙을 다시 정의하지 않는다. 이 표를 가리킨다.

---

## 2. 완료의 정의

| 상태 | 의미 |
| --- | --- |
| 코드 완료 | 작성과 빌드가 끝남. **아직 아무것도 확인되지 않음** |
| 검증 완료 | 사람이 게임에서 의도대로 동작함을 확인함 |

**"완료했습니다"는 검증 완료에만 쓴다.**

게이트는 커밋이 아니라 **머지**다. 피처 브랜치가 미검증 작업장 역할을 한다.

```
feature 브랜치 안  →  미검증 커밋 자유. 자주 커밋한다
main 으로 머지     →  검증 완료여야 한다    ← 게이트
```

미검증 상태로 커밋할 때는 메시지에 `미검증`을 명시한다. 머지는 `/land` 스킬로만 한다.
**머지해도 되는지는 사람이 판단한다** — `/land`를 호출하는 것이 그 판단이다.
스킬은 되묻지 않고, 커밋과 대화에서 읽어낸 검증 상태를 PR 본문에 남긴다.

빌드 후에는 **사람이 게임에서 무엇을 어떻게 확인하면 되는지** 구체적으로 알린다.

> 예: "빌드했습니다. 기지에 상자를 놓고 폭탄을 터뜨려 보세요. 상자는 살아남고,
> 근처 광석은 정상적으로 부서져야 합니다."

---

## 3. 역할 경계

AI가 못 하는 일은 **"실제로 게임을 해보는 것"**에 몰려 있다.

- **사람만 할 수 있는 것** — 게임 실행·플레이 테스트, 화면을 눈으로 보는 판단,
  유니티 GUI 조작, 최종 스프라이트 제작, 계정·약관 동의, 설계·밸런스 결정, **deploy 실행**
- 나머지(조사·코드·빌드·로그 분석·문서·커밋·PR·release)는 AI가 한다

자동화가 불가능한 지점을 발견하면 우회하려 애쓰기 전에 **먼저 알린다.**

---

## 4. 빌드

**윈도우에서만 된다.** 유니티 프로젝트는 윈도우 머신에 있고(경로는 `Editor/build.ps1`의
`-ProjectPath` 기본값), 맥 체크아웃은
문서·계획·코드 편집용 미러다. 맥 세션에서는 빌드를 시도하지 말고 사람에게 요청한다.

```
코드 수정 → 유니티 에디터 닫기 → powershell -File Editor/build.ps1
         → 게임 Mods 폴더 갱신 → 사람이 게임 재로드 → Player.log 확인
```

종료 코드: `0` 성공 / `1` 실패 / `2` 에디터가 열려 있음(닫고 재실행).
절차 상세는 `.claude/skills/build/`.

**사람을 부르기 전에 아래 셋을 전부 초록으로 만든다** (상세: `Editor/Docs/workflow.md`):

```
Editor/logictest.ps1   순수 로직. 초 단위, 유니티 없음   (사람 0)
Editor/build.ps1       컴파일 + 설치                      (사람 0)
Editor/verify.ps1      시스템 순서·동작 (ECS 하네스)      (사람 0)
                       ↓ 셋 다 초록일 때만
사람이 테스트 월드 1회 로드 → selfTest가 [NBZTEST] 줄로 판정   (사람 1)
```

유니티 테스트 러너(`-runTests`)는 이 머신에서 라이선스로 막힌다(exit 198). `-executeMethod`는 된다.

---

## 5. 금지

- **승인된 플랜 없이 파일을 수정하지 않는다** (§0)
- **오브젝트 ID를 변경하지 않는다.** 세이브에 기록되므로 기존 세이브가 손상된다
- **deploy(mod.io·창작마당 업로드)를 실행하지 않는다.** 준비까지 하고 멈춘다.
  전역 `/land-and-deploy`가 이걸 자동으로 해버리므로 그 스킬을 쓰지 않는다 (§8)
- 사람의 검증 없이 완료로 처리하지 않는다
- `design.md`의 결정을 임의로 바꾸지 않는다. 근거가 있으면 제안하고 승인을 받는다
- 결정 기록이 달린 항목을 "더 나은 방법"이라며 되돌리지 않는다. 이미 검토하고 뺀 것들이다

## 6. 멈추고 물어봐야 할 때

기획서에 없는 기능 / 기획서 결정을 바꿔야 할 근거 발견 / 플랜 범위 이탈 /
오브젝트 ID 변경 / **세이브 데이터 구조에 영향** / **조사 결과가 기획서 전제와 다름** /
**성능에 눈에 띄는 영향**. 마지막 셋은 조용히 넘어가면 나중에 훨씬 비싸진다.

---

## 7. 문서

세션이 바뀌면 맥락이 사라진다. **기록이 없으면 같은 조사를 반복한다.**

| 파일 | 내용 | 갱신 시점 |
| --- | --- | --- |
| `Editor/Docs/status.md` | 지금 몇 단계, 다음에 할 일 | **작업이 끝날 때마다** |
| `Editor/Docs/design.md` | 기획서 — 무엇을 만드는가 | 결정이 바뀔 때 (승인 후) |
| `Editor/Docs/workflow.md` | 개발 루프·검증 사다리 상세, 일하며 배운 것 | 절차가 바뀔 때 |
| `Editor/Docs/research.md` | 디컴파일·실험으로 알아낸 것 | 사실을 알아낼 때마다 |
| `Editor/Docs/coverage.md` | 검사가 초록이면 무엇까지 믿어도 되는가 | 테스트나 기능이 늘 때 |
| `Editor/Docs/store.md` | 스토어 업로드 문안·배포 전 점검·게임 속 확인 목록 | 배포할 때 |
| `Editor/Docs/failures.md` | 시도했으나 안 된 것과 이유 | 실패할 때마다 (아직 없음) |
| `Editor/GameData/` | DB 덤프 정적 참조 | 게임 업데이트 후 재생성 |

`status.md`는 **세션을 새로 시작한 사람이 이것만 읽고 이어갈 수 있어야** 한다.
문서 갱신도 작업이므로 §0의 플랜 규칙이 그대로 적용된다.

---

## 8. 깃 — 작업 사이클

```
플랜모드 → 승인 → 브랜치 생성 → 작업+커밋 반복 → 빌드 → 사람이 게임에서 검증
        → /land (PR 생성 → 머지 → 브랜치 정리) → main
        → (PR 누적) → /release (태그 + GitHub 릴리스)
```

### 브랜치 — 플랜 승인 직후 만든다

**플랜이 승인되면 첫 파일을 건드리기 전에 브랜치를 딴다.** 사람이 시키기를 기다리지 않는다.
브랜치명은 플랜 제목에서 뽑고 **만든 뒤 보고한다.**

`feat/…` 기능 · `fix/…` 버그 · `chore/…` 문서·설정·정리. `main`에 직접 커밋하지 않는다.

### 커밋 — 논리적 단위가 끝나면 즉시

작업이 다 끝나기를 기다리지 않는다. **의미 있는 단위 하나가 끝날 때마다 커밋한다.**
피처 브랜치 안이므로 미검증 상태여도 된다.

- 형식: `type: subject` + 본문. **제목과 본문 모두 영문** (§1-1)
- 본문에는 **무엇을 왜** 했는지. 판단 근거와 버린 대안도 남긴다
- 미검증이면 그렇게 적는다
- 한 커밋에 관련 없는 변경을 섞지 않는다. 머지 방식이 merge commit이라 개별 커밋이
  `main` 이력에 그대로 남는다

### PR·머지 — `/land` 로만

`main`에 코드가 들어가는 유일한 경로다. merge commit으로 머지한다(squash 아님).
호출 시점이 곧 사람의 머지 판단이므로 되묻지 않고, 검증 상태는 커밋과 대화에서 읽어 PR 본문에 남긴다.

PR **제목은 서술형 한 문장**이다. 커밋과 달리 `type:` 접두사를 붙이지 않는다 —
merge commit에서 PR 제목은 커밋 subject가 되지 않고, 커밋에는 이미 붙어 있다.

### 릴리스 — `/release` 로만

태그 + GitHub 릴리스까지다. **deploy(mod.io·창작마당)는 사람이 한다.**
둘을 한 명령에 묶지 않는다. deploy는 **release가 먼저 된 커밋에 대해서만** 한다 — 어떤 코드가
배포됐는지 항상 추적할 수 있어야 한다. 배포 전 점검과 업로드 문안은 `Editor/Docs/store.md`.

### 전역 gstack 스킬은 쓰지 않는다

- **`/land-and-deploy` — 절대 쓰지 않는다.** 머지 후 **배포까지 자동 실행**한다
  (deploy 전략 감지 → 배포 → 카나리 검증). §5의 deploy 금지를 정면으로 위반한다.
  트리거가 `"land"`·`"merge"`·`"land it"`이라 우리 `/land`와 겹치는 게 가장 위험하다.
  **"랜딩"·"머지"·"land"는 전부 이 저장소의 `/land`를 뜻한다.**
- `/ship` — `VERSION`·`CHANGELOG`를 전제하고, 테스트를 돌리고, 머지를 안 한다
