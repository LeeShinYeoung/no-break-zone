# NoBreakZone — 작업 규칙

Core Keeper 모드. 기획서는 `Editor/Docs/design.md`, 세부 규칙은 `.claude/rules/`.

---

## 0. 플랜 먼저

사용자가 "바로 해"처럼 명시하지 않는 한, 크기와 무관하게 **파일을 쓰기 전에 플랜 승인**을 받는다.

- 조사·읽기·질문은 승인 없이 한다.
- 결정할 항목이 남아 있으면 코드가 아니라 질문을 먼저 내놓는다. 질문에 답한 것은 승인이 아니다.
- 플랜에는 무엇을 바꾸는지, 건드릴 파일, **사람이 게임에서 무엇을 확인해야 하는지**, 위험,
  기획서의 어느 항목인지를 넣는다.
- 승인된 범위를 벗어나야 하면 멈추고 다시 묻는다.

## 1. 저장소 구조

저장소 루트가 곧 유니티의 `Assets/NoBreakZone/`, 즉 모드 번들 경로다.

1. **`.cs`·`.dll`·`Conf/`·`Localization/`·`Editor/`·`CodeGen/` 밖의 모든 자산이 번들에 실린다.**
   배포 대상이 아닌 것은 `Editor/` 아래에 둔다. 루트에 파일을 늘리지 않는다.
2. **모든 자산에 `.meta`가 붙는다.** 옮기거나 지울 때 `.meta`도 같이. 새 폴더는 폴더 `.meta`
   (`folderAsset: yes` + 새 GUID)와 함께 만든다.
3. 점(`.`)으로 시작하거나 `~`로 끝나는 폴더는 유니티가 읽지 않는다. `.meta`가 필요 없다.
4. **`Prefabs/`·`Textures/`·`Data/`는 손으로 고치지 않는다.** `Editor/genassets.py`가 덮어쓴다.
   그림 원본은 `Editor/Docs/art/`.
5. 파일·폴더명은 ASCII.

## 2. 언어

| 대상 | 언어 |
| --- | --- |
| 커밋·PR·릴리스 노트·README·LICENSE·CHANGELOG·코드 주석 | 영문 |
| `Editor/Docs/design.md` · `CLAUDE.md` · `.claude/**` | 한글 |

한국어를 모르는 사람이 먼저 보는 것은 전부 영문이다. 이미 한글로 쓰인 과거 커밋은 고치지 않는다.

## 3. 완료와 역할

- **코드 완료**는 작성·빌드가 끝났을 뿐 아무것도 확인되지 않은 상태다. **검증 완료**는 사람이 게임에서
  확인한 상태다. "완료했습니다"는 검증 완료에만 쓴다.
- 게이트는 커밋이 아니라 `main`으로의 머지다. 피처 브랜치 안에서는 미검증 커밋이 자유롭고, 메시지에
  미검증임을 적는다.
- 빌드 후에는 사람이 게임에서 **무엇을 어떻게** 확인할지 구체적으로 알린다.
- 사람만 하는 일: 게임 실행·플레이 테스트, 화면 판단, 유니티 GUI, 최종 스프라이트, 계정·약관,
  설계·밸런스 결정, deploy. 나머지는 AI가 한다. 자동화가 안 되는 지점은 우회하기 전에 먼저 알린다.

## 4. 빌드와 검증

**윈도우에서만 된다.** 맥 체크아웃은 편집용 미러라 빌드를 시도하지 않고 사람에게 요청한다.
경로는 `Editor/build.ps1`의 기본값에 있다.

사람을 부르기 전에 셋을 전부 초록으로 만든다.

```
Editor/logictest.ps1   순수 로직, 유니티 없음
Editor/build.ps1       컴파일 + 설치 (0 성공 / 1 실패 / 2 에디터 열림)
Editor/verify.ps1      시스템 순서·보호 동작
      ↓ 셋 다 초록일 때만
사람이 버릴 테스트 월드를 1회 로드 → selfTest가 [NBZTEST] 줄로 판정
```

유니티 테스트 러너(`-runTests`)는 라이선스로 막힌다(exit 198). `-executeMethod`는 된다.

## 5. 금지

- 승인된 플랜 없이 파일을 수정하지 않는다
- **오브젝트 ID를 바꾸지 않는다.** 세이브에 기록된다
- **deploy(mod.io·창작마당 업로드)를 실행하지 않는다.** 준비까지만 한다
- 사람의 검증 없이 완료로 처리하지 않는다
- 기획서의 결정을 임의로 바꾸지 않는다. 결정 기록이 달린 항목을 "더 나은 방법"이라며 되돌리지 않는다

## 6. 멈추고 물을 때

기획서에 없는 기능 / 기획서 결정을 바꿀 근거 / 플랜 범위 이탈 / 오브젝트 ID 변경 /
**세이브 구조에 영향** / **조사 결과가 기획서 전제와 다름** / **눈에 띄는 성능 영향**.

## 7. 깃

```
플랜 승인 → 브랜치 → 작업+커밋 → 빌드 → 사람이 게임에서 검증 → /land → main → (누적) → /release
```

- 플랜이 승인되면 첫 파일을 건드리기 전에 `feat/`·`fix/`·`chore/` 브랜치를 따고 보고한다.
  `main`에 직접 커밋하지 않는다.
- 논리적 단위마다 커밋한다. `type: subject` + 본문(무엇을 왜). 관련 없는 변경을 섞지 않는다 —
  merge commit이라 개별 커밋이 이력에 남는다.
- `main`에 넣는 길은 `/land` 하나다(merge commit, 서술형 PR 제목, 호출 = 사람의 머지 판단).
  태그·릴리스는 `/release`. deploy는 release된 커밋에 대해서만, 사람이 한다.
- **`/land-and-deploy`는 절대 쓰지 않는다.** 배포까지 자동 실행한다. "랜딩"·"머지"·"land"는 전부 이
  저장소의 `/land`다. `/ship`도 쓰지 않는다.
