# NoBreakZone — 작업 규칙

Core Keeper 모드. 상세 문서는 `Editor/Docs/` 아래에 있다.
읽는 순서: 이 문서 → `Editor/Docs/status.md` → `Editor/Docs/design.md`.

---

## 0. 최우선 규칙 — 플랜 먼저

**사용자가 "바로 해", "그냥 해"처럼 명시적으로 지시하지 않는 한, 어떤 작업이든
플랜모드를 거친다.** 작업의 크기·난이도는 판단 근거가 아니다. 한 줄 수정도 예외 없다.

- 조사·읽기·grep·질문은 승인 없이 해도 된다. 오히려 권장한다.
- **파일을 쓰는 순간부터** 승인된 플랜이 있어야 한다.
- 결정할 항목이 남아 있으면 코드가 아니라 질문을 먼저 내놓는다.
- **사용자가 질문에 답한 것은 구현 시작 승인이 아니다.** 승인은 플랜 승인뿐이다.
- 승인된 플랜의 범위를 벗어나야 하면 멈추고 다시 묻는다.

---

## 1. 이 저장소의 구조적 제약

저장소 루트가 곧 유니티의 `Assets/NoBreakZone/`, 즉 **모드 번들 경로(modPath)**다.
이게 파일을 어디에 둘지를 전부 규정한다.

1. **modPath 아래 거의 모든 자산이 번들에 실린다.** `ModBuilder`는 `.cs`·`.dll`·`Conf/`·
   `Localization/`·`Editor/`·`CodeGen/`을 **제외한 전부**를 AssetBundle로 쓸어담는다.
   → **배포 대상이 아닌 것은 `Editor/` 아래에 둔다.** 루트에는 파일을 늘리지 않는다.
   (근거: `Editor/Docs/research.md` 6장. 실제로 초안 PNG와 `sprites.py`가 번들에 실려 사고가 났다.)
2. **모든 자산에 `.meta`가 붙는다.** 이름을 바꾸거나 옮길 때는 `.meta`도 **반드시 같이**
   옮긴다. 안 그러면 유니티가 GUID를 새로 발급해 참조가 끊긴다. 새 폴더를 만들면 폴더
   `.meta`도 함께 만든다(`folderAsset: yes` + 신규 GUID).
3. **점(`.`)으로 시작하는 폴더는 유니티가 자산으로 읽지 않는다.** `.claude/`·`.github/`는
   modPath 안에 있어도 번들과 무관하고 `.meta`도 필요 없다.
4. **파일·폴더명은 ASCII.** 본문은 한글로 써도 된다. 맥/윈도우를 오가는 저장소라
   한글 경로는 유니코드 정규화 사고를 낸다.

---

## 2. 완료의 정의

| 상태 | 의미 |
| --- | --- |
| 코드 완료 | 작성과 빌드가 끝남. **아직 아무것도 확인되지 않음** |
| 검증 완료 | 사람이 게임에서 의도대로 동작함을 확인함 |

**"완료했습니다"는 검증 완료에만 쓴다.** 게임플레이에 영향을 주는 변경은 사람의 인게임
검증 전에 커밋하지 않는다. 커밋해야 할 사정이 있으면 커밋 메시지에 `미검증`을 명시한다.

빌드 후에는 **사람이 게임에서 무엇을 어떻게 확인하면 되는지** 구체적으로 알린다.

> 예: "빌드했습니다. 기지에 상자를 놓고 폭탄을 터뜨려 보세요. 상자는 살아남고,
> 근처 광석은 정상적으로 부서져야 합니다."

---

## 3. 역할 경계

AI가 못 하는 일은 **"실제로 게임을 해보는 것"**에 몰려 있다.

- **사람만 할 수 있는 것** — 게임 실행·플레이 테스트, 화면을 눈으로 보는 판단,
  유니티 GUI 조작, 최종 스프라이트 제작, 설계·밸런스 결정, **deploy 실행**
- 나머지(조사·코드·빌드·로그 분석·문서·커밋·PR·release)는 AI가 한다

자동화가 불가능한 지점을 발견하면 우회하려 애쓰기 전에 **먼저 알린다.**

---

## 4. 빌드

**윈도우에서만 된다.** 유니티 프로젝트는 `C:\Unity\CoreKeeper`에 있고, 맥 체크아웃은
문서·계획·코드 편집용 미러다. 맥 세션에서는 빌드를 시도하지 말고 사람에게 요청한다.

```
코드 수정 → 유니티 에디터 닫기 → powershell -File Editor/build.ps1
         → 게임 Mods 폴더 갱신 → 사람이 게임 재로드 → Player.log 확인
```

종료 코드: `0` 성공 / `1` 실패 / `2` 에디터가 열려 있음(닫고 재실행).
절차 상세는 `.claude/skills/build/`.

---

## 5. 금지

- **승인된 플랜 없이 파일을 수정하지 않는다** (§0)
- **오브젝트 ID를 변경하지 않는다.** 세이브에 기록되므로 기존 세이브가 손상된다
- **deploy(mod.io·창작마당 업로드)를 실행하지 않는다.** 준비까지 하고 멈춘다
- 사람의 검증 없이 완료로 처리하지 않는다
- `design.md`의 결정을 임의로 바꾸지 않는다. 근거가 있으면 제안하고 승인을 받는다
- 결정 기록이 달린 항목을 "더 나은 방법"이라며 되돌리지 않는다. 이미 검토하고 뺀 것들이다

## 6. 멈추고 물어봐야 할 때

기획서에 없는 기능 / 기획서 결정을 바꿔야 할 근거 발견 / 플랜 범위 이탈 /
오브젝트 ID 변경 / **세이브 데이터 구조에 영향** / **조사 결과가 기획서 전제와 다름** /
**성능에 눈에 띄는 영향**. 마지막 셋은 조용히 넘어가면 나중에 훨씬 비싸진다.

---

## 7. 문서

세션이 바뀌면 맥락이 사라진다. **기록이 없으면 같은 조사를 반복한다.**

| 파일 | 내용 | 갱신 시점 |
| --- | --- | --- |
| `Editor/Docs/status.md` | 지금 몇 단계, 다음에 할 일 | **작업이 끝날 때마다** |
| `Editor/Docs/design.md` | 기획서 — 무엇을 만드는가 | 결정이 바뀔 때 (승인 후) |
| `Editor/Docs/workflow.md` | 일하는 방식 — 이 문서의 원본 | 절차가 바뀔 때 |
| `Editor/Docs/research.md` | 디컴파일·실험으로 알아낸 것 | 사실을 알아낼 때마다 |
| `Editor/Docs/failures.md` | 시도했으나 안 된 것과 이유 | 실패할 때마다 (아직 없음) |
| `Editor/GameData/` | DB 덤프 정적 참조 | 게임 업데이트 후 재생성 |

갱신은 `/log` 스킬로 한다. `status.md`는 **세션을 새로 시작한 사람이 이것만 읽고
이어갈 수 있어야** 한다.

---

## 8. 깃

- 브랜치: `main`은 항상 동작하는 상태. 작업은 `feat/` · `fix/` · `chore/`
- 커밋: **영문 `type: subject`** + 한글 본문. 무엇을 왜 했는지가 드러나게
- 혼자여도 PR을 만든다. 본문에 **사람이 게임에서 확인한 것**을 적는다
- release(태그·깃허브 릴리즈)와 deploy(mod.io·창작마당)는 다르다. **한 명령에 묶지 않는다**
- commit/pr/release는 전역 gstack 스킬(`/ship`, `/review`)을 쓴다. 이 문서의 제약이 우선한다
